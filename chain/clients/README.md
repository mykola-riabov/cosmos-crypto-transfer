# Ledger clients

`ledger_clients.py` создаётся через меню **Check and create data → Generate ledger clients**.

Файл не хранится в git. После генерации здесь появятся `LedgerClient` для каждой сети из `cosmos_data_list.json`.
