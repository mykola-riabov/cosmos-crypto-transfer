# Optional path overrides. Defaults:
#   project = directory with menu_crypto.py
#   source  = ./source (inside project)
#   secrets = ~/.market_ai_secrets/cosmos-crypto-transfer/

# COSMOS_PROJECT_ROOT=/path/to/cosmos-crypto-transfer
# COSMOS_SOURCE_PATH=/path/to/source
# MARKET_AI_SECRETS_SLUG=cosmos-crypto-transfer
